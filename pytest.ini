[pytest]
pythonpath = .
addopts =
    --strict-markers
    --strict-config
    -ra
testpaths = tests
markers =
    unit: fast unit tests
    integration: tests that require integrations or containers
    smoke: minimal validation checks
junit_family = xunit2
junit_suite_name = opsledger-platform